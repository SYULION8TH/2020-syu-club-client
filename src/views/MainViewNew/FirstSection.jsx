import React from 'react';

import './scss/MainFirstSection.scss';

const FirstSection = ({ className }) => {
    return <div className={`__main-view-first-section-container ${className}`}></div>;
};

export default FirstSection;
