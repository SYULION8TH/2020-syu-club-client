import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

const StackedClubList = (props) => {
    return <div className={`__stacked-club-list-container `}>
        
    </div>;
};

StackedClubList.propTypes = {
    className: PropTypes.string,
};

export default StackedClubList;
